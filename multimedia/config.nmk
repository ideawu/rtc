BASE_DIR = $(ROOT)
DIST_LIB_DIR = $(ROOT)\..\dist\lib
DIST_OBJ_DIR = $(ROOT)\..\dist\obj
DIST_INC_DIR = $(ROOT)\..\dist\include/rtc

TARGET_ARCH=x86
OS=win
C=cl
CC=cl

CFLAGS =
CFLAGS = $(CFLAGS) /EHsc
CFLAGS = $(CFLAGS) -I $(BASE_DIR)
CFLAGS = $(CFLAGS) -D WEBRTC_CLOCK_TYPE_REALTIME -DWEBRTC_NS_FIXED

